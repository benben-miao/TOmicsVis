# Validate is data.frame

Validate input is a data.frame, stop with error if not

## Usage

``` r
validate_is_dataframe(x, param_name = "x")
```

## Arguments

- x:

  Object to validate

- param_name:

  Character: parameter name for error message

## Value

Logical: TRUE if valid (invisible)
